# CRA dev server — 3D monitor iframe (MonitorScreen).
# Must match IFRAME_OS_URL in src/Application/World/MonitorScreen.ts
PORT=3020
BROWSER=none
# Contact form: Express server at portfolio root (pnpm start → :8080)
REACT_APP_CONTACT_API=http://localhost:8080/api/send-email
